This site uses Apache as the server backend, together with the mod_wsgi module for running python code.

The code uses bottle as the python web framework together with bottle_mysql for easier connecting to the database.